import styled from 'styled-components';
import { FC } from 'react';
import { TConsoleOutput } from '@lib/shared';

const Wrapper = styled.div``;

export const ConsoleOutput: FC<TConsoleOutput> = ({}) => {
  return <Wrapper></Wrapper>;
};
